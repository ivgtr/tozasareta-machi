# 現状と引き継ぎ

次のセッションで作業を続けるための現状把握ドキュメント（`AGENTS.md` がここへ誘導する）。`docs/README.md`（設計ドキュメント目次）とあわせて読むこと。

## プロジェクト概要

**孤立した町の30日間** — ブラウザで動くターン制運営シミュレーション。災害で孤立した町の臨時対策責任者として、ユニット（人員）を任務に配置し、30日間の再接続を耐え抜く。

- 技術: Vite + React 19 + TypeScript(strict) + Vitest
- 詳細設計: `docs/01`〜`docs/16`（`docs/README.md` が目次）

## 現在の状態

### 完成済み
- **v1（P1–P9）**: コアシステム（純粋 reducer / 30日 / 4エンディング / イベント / セーブ）
- **v1.1（U1–U3）**: ユニットシステム（ユニークユニット / 統合到着 / スポットライトUX）
- **選択型イベント（docs/16 P1+P2）**: 決定フェーズ + 交易/危機/探索の選択イベント（詳細は下記）
- **ユニーク30体（パロディ）**: 実在人物（故人）15 + アニメ/ゲームキャラ15。初期4体はオリジナル（真壁史子/榊直人/森レナ/岩倉源造、`SAVE_VERSION=4`）
- **アート導入済み**: イベント/エンディング/アイコン/背景/肖像（ユニーク30+汎用8）/スカイライン（状態別3バリアント）
- **品質**: テスト88件 / typecheck / lint / build すべて緑

### 選択型イベント（今セッションの主成果）
- **決定フェーズ**: 選択イベント発火で `phase:'choice'` に一時停止、`resolveChoice` アクションで再開。純粋・決定的を維持（`pendingEvents` キュー方式）
- **交易/危機**: 資源の弁（トレード）/トレードオフを選ぶ。見送り・断るも delta0 効果で本部記録に残る
- **探索（再設計済み）**: 備蓄前払い（`cost:12`）で送出 → **数ターン不在**（労力不可・食料消費0）→ **帰還は毎日抽選**（最短 `minDays` 日後から、日数で帰還率上昇・ランダム）→ 成果は**適性で決定**（大成功=4日分 5-10% / 成功=1日分 30-60% / 危険=負傷・死亡 / 失敗）。報酬は帰還日数ボーナス+分散（×0.7-1.3）。探索中は探索イベント非発生
- **探索中ユニットは清算から除外**: 食料消費/餓死/離脱/popular・troublemaker士気/回復のいずれも不在ユニットを計上しない（表示も StatusWall・TopBar で整合）
- **経済をイベント主軸へ**: 士気→生産倍率（低士気で生産低下）+ 任務係数引き締め（1.0）

### バランス（実測・200戦）
- **熟練〜80% / 無作為〜2-4%**（イベント依存・硬派、全4エンディング到達可）
- ⚠️ 調整過程では熟練70%を狙っていたが、探索再設計後に〜80%へ drift。sim の熟練は「低適性ユニットを探索に送る」ヒューリスティック（proxy）。再調整は任意（`balance.ts` の `expedition` / `effect` を触る）

### アセット状況（`src/ui/assets/`）

| 種別 | 状況 |
| --- | --- |
| `event/` | 導入済み（23枚。うち `refugees.png` は arrival 置換で**孤立**——削除or再利用候補） |
| `ending/` | 導入済み（4枚） |
| `icon/` | タスク+状態+資源アイコン導入済み。資源4枚（food/power/medical/morale）は StatusWall のゲージに接続済み |
| `scene/` | `night.png` 導入済み |
| `skyline/` | 状態別3バリアント（town_normal/town_dark/town_danger）のPNGを導入済み。未着時は村風プロシージャルSVG |
| `portrait/` | 導入済み（ユニーク30体+汎用8体）。汎用は決定的割り当て、ユニークは固定ID |

## 実行・検証コマンド

```bash
npm install
npm run dev          # プレイ（ブラウザ）
npm test             # Vitest 全88件
npm run typecheck    # tsc --noEmit
npm run lint         # ESLint（game/ のDOM非依存をlintで強制）
npm run build        # 本番ビルド
npm run sim          # バランス測定（無作為生存率・ending分布をログ出力）
```

## アーキテクチャ

```
src/game/     ゲームコア（DOM非依存・純粋。ここが心臓部）
  types.ts      Unit/Effect/GameState/EventDef/ChoiceOption 等の型
  engine.ts     step(state, action) => { state, effects }（純粋 reducer）。commitDay と resolveChoice の二系統
  actions.ts    任務の効果計算（適性駆動）/ preview / autoAssign / isOnExpedition
  settlement.ts 日次清算（食料=在籍人員×4/減衰/負傷/死亡/離脱/成長/特性/探索帰還解決）。探索中ユニットは除外
  events.ts     イベント発火（自動+選択の二軸抽選）/ choiceOptions（perUnit動的選択肢）
  ending.ts     崩壊判定 / 4エンディング評価
  rng.ts        シード付き乱数（決定性の要）
  traits.ts     7特性の定義
  data/
    balance.ts  全数値（BALANCE。調整はここだけ触る。expedition セクション含む）+ SAVE_VERSION=4
    units.ts    初期4人 + ユニーク30体（パロディ）+ ランダム生成 + 汎用肖像プール
    events-data.ts  全イベント定義（arrival=統合到着抽選、expedition/trade_offer/power_crisis/stockpile_crisis=選択イベント）
src/ui/       React UI
  screens/    TitleScreen / PlayScreen
  components/ UnitCard, DecisionBoard, StatusWall, *Overlay 群 等
  hooks/      usePlayback（ビート再生）, useTokenDnD（Pointer Events DnD）
  art/        PixelArt + manifest（アートスロット。PNGを自動検出）
  assets/     PNG アセット（{kind}/{id}.png に置くと自動反映）
  store.ts    zod スキーマ + セーブ整合性 / StoreProvider / settings
tests/        Vitest（gameコア単体・決定性・simulation・UIスモーク・playback）
```

### 中核の契約

- `step(state, action)` は**純粋**。同じ seed + 同じ行動 = 同じ結果（決定性テストで保証）。
- 1日の解決は `commitDay` → 任務効果 → 清算（探索帰還解決含む）→ イベント → 日付進行。
- **選択イベント**: 発火すると `phase:'choice'` で一時停止し、`resolveChoice` アクションで再開する。探索は `perUnit` で動的選択肢を生成。
- 再生（`usePlayback`）は**ビート単位**: `flow`（定常増減=レポートへ流す）/ `event` / `arrival`（後者は**スポットライト=ポーズ+カード+続行**）。イベントはすべてポーズする統一UX。

## 主要な設計決定（要約）

- **作業員→ユニークユニット**: 名 + 二つ名 + 4適性(労/技/医/魅) + 特性 + フレーバー。配置は「誰をどの任務へ」の一段。
- **ユニークパロディ**: 30体を実在人物（故人）15 + アニメ/ゲームキャラ15のパロディに。名前は1文字ズラし、フレーバーは元ネタ参照。初期4体はオリジナルのまま。
- **統合到着抽選**: 30体ユニークプール + ランダム生成の混合抽選。重みは日数逓増（`arrivalBase + day×arrivalPerDay`）、確定受け入れ、上限10、ユニークは重複なし（`flags.joinedUniques`）。
- **ランダム名**: 和風単名/姓名/西洋名の混合生成。二つ名はピーク適性由来（力自慢/発明家/薬草通/人たらし）。
- **汎用肖像**: ランダム加入者に8枚プールから決定的割り当て（`selectRandomPortrait`、seed+unitId ハッシュ、RNG非消費）。未使用優先、枯渇時は全復活。
- **ユニーク判定**: `unique` フラグ（二つ名の有無ではない）。カードの二つ名行は常時表示で高さ統一。
- **選択型イベント**: `EventDef.kind:'choice'` + `choices`/`perUnit`。発火で `phase:'choice'` 停止、`resolveChoice` で再開。探索は `perUnit` で動的選択肢（ユニットごとに生成）。
- **探索の経済**: 備蓄前払い・数ターン不在・毎日抽選帰還・適性で大成功(4日分)/成功(1日分)。不在ユニットは清算・表示から除外。
- **バランス**: `BALANCE`（`src/game/data/balance.ts`）に一元化。実測 熟練〜80%/無作為〜2-4%（イベント依存・硬派）。

## 次のステップ（未着手・候補）

1. **バランスの再調整（任意）**: 実測は熟練〜80%（調整過程の目標は70%）。`balance.ts` の `expedition`（成功率・報酬・帰還率）/ `effect`（任務係数）で調整。sim は `tests/simulation.test.ts` の GAMES を増やして測る（熟練は低適性ユニットを探索に送るヒューリスティック）
2. **実プレイテストのフィードバック反映**: 選択型イベント（交易/危機/探索）のUX・難易度感の確認
3. **エンディングの成果/成績ベース分岐**（`docs/16 §8` P3・延期済み）: 誰も死なない/◯人死亡/特定ユニーク/成績集計で分岐
4. **孤立アセット整理**: `event/refugees.png` は arrival 置換で未使用（削除or再利用）
5. 拡張（`docs/04`: 地区/派閥/交易/外交 等）

## 規律・注意点

- **フェーズごとにコミット**（`docs/11` 参照。1フェーズ=1コミット、CI緑を条件に）
- **数値は `balance.ts` に集約**。ロジックにマジックナンバーを書かない
- **コードにコメントは原則追加しない**
- **韓国語混入に注意**（過去に `소식`/`균형`/`바로` が混入した）。懸念があれば `[가-힣]` でgrep
- セーブは zod スキーマ（`SAVE_VERSION=4`）。非互換の旧セーブは破棄→新規ゲーム
- `src/game/` は DOM非依存（lintで強制）。UIからコアへの一方向依存を守る
