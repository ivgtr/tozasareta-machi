# 現状と引き継ぎ

次のセッションで作業を続けるための現状把握ドキュメント（`AGENTS.md` がここへ誘導する）。`docs/README.md`（設計ドキュメント目次）とあわせて読むこと。

## プロジェクト概要

**孤立した町の30日間** — ブラウザで動くターン制運営シミュレーション。災害で孤立した町の臨時対策責任者として、ユニット（人員）を任務に配置し、30日間の再接続を耐え抜く。

- 技術: Vite + **Phaser 3.90** + TypeScript(strict) + Vitest（Issue #1 P3 で **React は撤去済み**）
- 詳細設計: `docs/01`〜`docs/25`（`docs/README.md` が目次）

## 現在の状態

**最新: Issue #1 町中心UI（Phaser 移行）が P5 まで完了・P6 入口で停止中。** 現行ビルドは Phaser 版（React は撤去済み・本番エントリ切替済み・**未 push**）。ゲーム内容は v1.4 から不変。D5（予告・連鎖の拡張）のみ未着手。詳細は下記「Issue #1 実行記録」。

- **バランス（sim 実測・v1.4 から不変）**: 熟練 53%（32/60）/ 無作為 0/40（400ゲーム測定の真値 1/400）。イベント43+4件。`foodPerUnit:4.5` / `food.decay:3`
- **FR 番人**: 19/60（`>=10` は維持）
- **品質**: テスト179件 / typecheck / lint / format:check / build すべて緑
- **セーブ**: `SAVE_VERSION=5`（全工程で型変更なし・据え置き）
- **未解決のユーザー報告**: 「UI修正が反映されていない」（下記「次のステップ」1 の診断手順を参照。dev 両ポートでの配信とヘッドレス描画は確認済み）
- **次の作業**: 下記「次のステップ」参照（引き継ぎはこのドキュメントが正。docs/21 は v1.4 作業過程の歴史的資料）

### バグ修正セッションの決定記録（2026-08）

以下は**決定**として扱う。計画書の記述に合わせた是正と、計画書に無い契約の明文化の2種。将来の作業はこれを正とし、理由なく元の挙動へ戻さない（戻す場合はこの一覧を更新すること）。

| # | 決定 | 根拠 | 計画書との関係 | 検証 |
| --- | --- | --- | --- | --- |
| 1 | `sanitizePlan` は探索中ユニットを配置から除外（エンジン契約）。narrow トレイの探索中トークンは wide と対称に無効化・減光・「探」バッジ | 不在ユニットが産出・XP・負傷ロールに参加していた（「不在ユニットは労力不可」違反）。UI ガードは wide のみで narrow が漏れ | docs/16 §3「任務配置不可」をエンジンで強制化 | 回帰テスト3件 |
| 2 | `stockpile_crisis.distribute` に `when: stockpile >= 15` を追加 | 同日の自動イベント（storage_flood 等）で備蓄が減った後も選択でき、持たない備蓄を支払って士気+12 | docs/16 §3「choices を when で絞る」を全コスト選択肢に適用（trade_offer は従来どおり） | 回帰テスト2件（境界14/15） |
| 3 | 調達トグルは ON でも不足マーク（`（不足）`/`!`）を表示 | 不足時に ON にすると確定で `sanitizePlan` が黙って取り下げ「反映されない」見た目になる | UI 表示のみ（ボタン無効化は配置追加後に OFF へ戻せなくなるため不採用） | ヘッドレス差分 |
| 4 | 低食料判定の人員数は清算完了時（帰還・死亡後）の在籍数を使う | 探索帰還で復帰したユニットが閾値に数えられず lowFood ペナルティが発火しなかった（HUD の `deriveAlerts` と不一致） | バランス影響なし（sim: 熟練52%±6pt / 無作為0/40 / FR19/60、基準と同値） | 回帰テスト1件 + sim |
| 5 | `phase:'choice'` で `pendingChoice` がない状態は `resolveChoice` で planning へ回復 | 全操作が拒否されデッドロック（zod スキーマはこの不変条件を強制しないため、壊れたセーブで発生しうる） | 計画書に無い回復パス（正常プレイでは到達不能。既存の `findEvent` 欠落時回復と同パターン） | 回帰テスト5件 |
| 6 | アクト遷移 effect の `day` は解決中の日付（`s.day`）でスタンプ | 新日付でスタンプされ report に日付が混在、ログ見出しが1日ずれる | docs/20 の「遷移 effect 1本」の実装詳細（`day` はログ表示のみに使用） | 回帰テスト1件 |
| 7 | `storeReducer.resolveChoice` は no-op 時に同一オブジェクトを返す（commitDay/undo と対称） | 不要なセーブ書き込み・リスナー通知を防ぐ | 内部整合のみ（公開API・セーブ形式に影響なし） | 回帰テスト5件 |

**対象外の判断（仕様・到達不能）**:

- 選択イベントの `when` は**発火時のみ**判定し、先行する自動イベント適用後には再判定しない（docs/16 §3 どおり「発火時、choices を when で絞る」。自動イベントで条件が変わっても選択肢の `when` だけで絞る）。再判定の追加は仕様変更になるため採用しない
- `buildBeats` は同一 source（`event:expedition_return`）を1ビートにまとめる。同時探索は1体制限のため複数帰還は現行ルールで発生しない（将来同時探索を導入する場合はビート分割が必要）

### 完成済みバージョン履歴

| バージョン | 内容 | 設計書 |
| --- | --- | --- |
| v1（P1–P9） | コアシステム（純粋 reducer / 30日 / 4エンディング / イベント / セーブ） | 06 |
| v1.1（U1–U3） | ユニットシステム（ユニークユニット / 統合到着 / スポットライトUX / 肖像画） | 12–14 |
| 選択型イベント | 決定フェーズ + 交易/危機/探索の選択イベント。経済をイベント主軸へ | 16 |
| v1.2（M1–M4） | 期間修正（Modifier）機構 + 自動イベント22件 | 17 |
| ゲーム画面UI改善 | ゲームメニュー / モバイル資源HUD / 配置操作の明確化 / 低強調CommitBar | 18 |
| v1.3（E1–E4） | 経済再編とプレイ感改善（次節） | 19 |
| v1.4（D1–D4） | アクティブ難易度: 派生脅威度 + アクト制（次々節） | 20 |
| **Issue #1（P1–P5）** | **町中心UI: Phaser 3 フル移行+アイソメ町（プレースホルダ）。P6 未完了・下記実行記録参照** | 22–24 |

- **ユニーク30体（パロディ）**: 実在人物（故人）15 + アニメ/ゲームキャラ15。初期4体はオリジナル（真壁史子/榊直人/森レナ/岩倉源造）
- **アート導入済み**: イベント/エンディング/アイコン/背景/肖像（ユニーク30+汎用8）/スカイライン（状態別3バリアント）。詳細は「アセット状況」

### v1.3 の詳細（最新作業）

方針: 「**予算=唯一の希少ハブ / 食料=漏れるバケツ / 備蓄=明日のために貯める対象 / 士気=予算競争の一員**」。

- **E1 コア経済**: 食料減衰 `food.decay:5`（power/medical と同じ流儀の固定値。道路復旧が「食料メンテ」になり永続解決を解消）/ 士気基底減衰 `morale.decay:2`（calm+2 と均衡し安定時は差し引き0。放置すると prodHighAt(50) を割り生産低下）/ 炊き出しコストを 備蓄15 → 予算15 に変更（士気が電力・医療と予算競争に入る）/ 常設トグル「備蓄を調達」`procure`（予算15→備蓄12、`DayPlan.procure`、ration と対）/ trade_offer に `buy_stockpile`（予算15→備蓄+12）
- **E1 探索ガード**: expedition の `when` に `stockpile >= cost` を追加。実行不能時の発火を止める
- **E2 帰還スポットライト**: 探索帰還 effect の source を `event:expedition_return` に変更し `unit:{id}` ターゲットを混在 → 再生機構が自動で arrival スポットライト化（ポーズ+カード+続行）。死亡時は unit ターゲットなしのイベントビート。ArrivalOverlay は帰還時 kicker「探索から帰還した」
- **E3 UI**: DecisionBoard に「備蓄を調達」トグル（炊き出しは予算表示へ自動追随）
- **E4 バランス確定**: `foodPerUnit:4→5`（人員比例の食料消費。終盤に人員が増える熟練にのみ効くデカップリングレバー）。**検討したが不採用**: 食料瀕死時の救済イベントは、食料逼迫構成では熟練も閾値に到達して勝率を押し上げたため削除（詳細は docs/19 冒頭）
- **full_recovery の到達性（解決済み）**: v1.3 の食料圧の下で autoAssign は 0 だが、**狙ったプレイでは到達可**（`tests/full-recovery.test.ts`: 熟練simと同じseedで **37/60** 到達）。バランス無変更で決着。詳細は docs/19 §7

### v1.4 の詳細（最新作業）

方針: 「**B（アクト制）が公平性の背骨、A（派生脅威度）が応答層**」。イベント総数は増やさず、1日1件の自動イベント抽選の重み構成だけを動かす。純粋・決定的を維持（新規の永続 state なし、`SAVE_VERSION=5` 据え置き）。

- **A 派生脅威度**: `threat.ts` の `threatLevel(state) = actBase[act] + slackCount(state)`。slack は5条件（食料6日分 / 電力75 / 医療75 / 士気75 / 備蓄55、各+1、最大5）。`determineDayEvents` が threat トーンの自動イベントの weight を `×min(1 + 0.2×level, 2.0)` でスケーリング（boon は不変、arrival は対象外。tone 分類は threat 22 / boon 20）
- **B アクト制**: 11–20日=膠着期（`decay:power ×1.1`）/ 21–30日=正念場（`decay:power ×1.25` / `decay:medical ×1.15` / `income:budget ×0.9`）。`finalizeDay` が現在日のアクト Modifier を**冪等に追加**（旧セーブの途中再開も初回 finalize で補完）。境界日のみ遷移 effect（flow ビート + StatusWall バッジ「膠着期」「正念場」）
- **sim 前後比較（docs/21 ベースライン比）**:

| 項目 | v1.3 | v1.4 round 1 | v1.4 確定 |
| --- | --- | --- | --- |
| 熟練（60ゲーム） | 28/60（47%） | 18/60（30%） | **32/60（53%）** { MS:12, SG:20, C:28 } |
| 無作為（40ゲーム） | 1/40（3%） | 0/40 | **0/40**（400ゲーム真値 1/400。v1.3 真値も 3/400） |
| FR 狙い撃ち（60ゲーム） | 37/60 | 11/60 | **19/60**（番人 `>=10` は緑） |
| threat 発火率（熟練・アクト別） | — | 44/58/57% | **42/57/64%**（エスカレーション確認） |

- **確定プロセスの知見（D4）**: ① fpu/decay の緩和は熟練を大きく引き上げる（v1.3 の知見を再確認）。② ゲート方式（食料バッファ有のランだけ課税）は、autoAssign が食料ギリギリで戦うため**逆効果**だった。③ アクト倍率の強化は熟練より FR を先に壊す（FR は絶対値、autoAssign は分散で耐える）。④ slack のゲージ条件（士気等）は崩壊直前のランも到達するため課税の選別度が低い。⑤ 無作為の生存は配置由来の食料収入で決まり、バランスレバーでは 5% に届かない（v1.3 の「3%」も40ゲームの標本誤差）。レッドライン（熟練≤60% / FR≥10 / slack不変条件）はすべて維持
- **未着手（D5・任意）**: typhoon 予報 / landslide_warning→actual の応答窓 / アクト HUD ラベルの強化（バッジ自体は実装済み）

### ゲームシステム要点（現行）

- **期間修正（Modifier）**: `GameState.modifiers` に期間付き効果を保持。`modifiers.ts` のクエリ（`queryMult`/`queryAdd`/`isTaskDisabled`/`tickModifiers`）で settlement/actions が参照。ターゲット文字列（`consume:food`/`decay:power`/`produce:{task}`/`produce:all`/`income:budget`/`drain:stockpile` 等）×演算（mult/add/set）の汎用設計。任務ロックは `produce:{task}` set 0。**アクト Modifier**（`act_stalemate`/`act_final`）は finalizeDay が毎日存在保証（冪等・途中再開補完）
- **派生脅威度**: `threat.ts`（`actOf`/`slackDetail`/`slackCount`/`threatLevel`/`threatWeightMult`）。`determineDayEvents` が threat トーンの実効 weight をスケーリング（`BALANCE.threat`）。`EventDef.tone`（threat/boon）は定義側のみで GameState/セーブ無影響
- **選択型イベント**: `EventDef.kind:'choice'`。発火で `phase:'choice'` に一時停止、`resolveChoice` で再開。選択肢は `when` でフィルタ。探索は `perUnit` で動的選択肢
- **探索**: 備蓄前払い（`cost:12`）で送出 → 数ターン不在（労力不可・食料消費0）→ 毎日帰還抽選（`minDays` 後から、日数で帰還率上昇）→ 適性で成果決定（大成功=4日分/成功=1日分/危険=負傷・死亡/失敗）。不在ユニットは清算・表示から除外
- **経済**: 士気→生産倍率（`moraleMult`、prodHighAt 50）。予算収入 = `income:30` + `bonus:15`（電力≥`bonusAt:50`）。食料は毎日 `food.decay:5` 減衰 + 在籍人員×`foodPerUnit:5` 消費

### アセット状況（`src/ui/assets/`）

| 種別 | 状況 |
| --- | --- |
| `event/` | 既存20件は導入済み（PNGあり）。v1.2新規22件は**未制作**（プレースホルダ表示。対応表は docs/08 §4.5）。孤立4枚（refugees/retired_medic/stranded_engineer/young_volunteer）は再利用候補 |
| `ending/` | 導入済み（4枚） |
| `icon/` | タスク+状態+資源アイコン導入済み。資源4枚（food/power/medical/morale）は StatusWall のゲージに接続済み。`procure` アイコンは manifest 登録済み（PNG未制作→グリフ表示） |
| `scene/` | `night.png` 導入済み |
| `skyline/` | 状態別3バリアント（town_normal/town_dark/town_danger）のPNGを導入済み。未着時は村風プロシージャルSVG |
| `portrait/` | 導入済み（ユニーク30体+汎用8体）。汎用は決定的割り当て、ユニークは固定ID |

## 実行・検証コマンド

```bash
npm install
npm run dev          # プレイ（ブラウザ）
npm test             # Vitest 全161件
npm run typecheck    # tsc --noEmit
npm run lint         # ESLint（game/ のDOM非依存をlintで強制）
npm run build        # 本番ビルド
npm run sim          # バランス測定（生存率・ending分布・イベント発火/modifier統計をログ出力）
```

- sim の `console.log` は **`npm run sim -- --reporter=verbose`** でないと表示されない
- 400ゲーム測定（真値の推定）は `tests/simulation.test.ts` の `const GAMES = 40` を一時的に変更して `npx vitest run tests/simulation.test.ts --testNamePattern 無作為 --reporter=verbose`（コミット前に必ず戻す）

## アーキテクチャ

```
src/game/     ゲームコア（DOM非依存・純粋。ここが心臓部）
  types.ts      Unit/Effect/GameState/EventDef/ChoiceOption/Modifier/DayPlan 等の型
  engine.ts     step(state, action) => { state, effects }（純粋 reducer）。commitDay と resolveChoice の二系統。finalizeDay で modifier をtick
  actions.ts    任務の効果計算（適性駆動・modifier倍率）/ taskCost / preview / sanitizePlan / autoAssign（disabled任務回避）/ isOnExpedition
  settlement.ts 日次清算（食料=在籍人員×foodPerUnit・食料減衰・士気基底減衰・procure・modifier消費倍率/負傷/死亡/離脱/成長/特性/探索帰還解決/drain）。探索中ユニットは除外。帰還 effect は EXPEDITION_RETURN_SOURCE
  modifiers.ts  期間修正のクエリ（queryMult/queryAdd/isTaskDisabled/tickModifiers/addModifier）
  events.ts     イベント発火（自動+選択の二軸抽選）/ choiceOptions（perUnit動的選択肢・whenフィルタ）
  ending.ts     崩壊判定 / 4エンディング評価
  threat.ts     派生脅威度（actOf/slackDetail/slackCount/threatLevel/threatWeightMult。v1.4）
  rng.ts        シード付き乱数（決定性の要）
  traits.ts     7特性の定義
  data/
    balance.ts  全数値（BALANCE。調整はここだけ触る。food/morale減衰・procure・expedition/modifier セクション + threat/acts セクション（v1.4）を含む）+ SAVE_VERSION=5
    units.ts    初期4人 + ユニーク30体（パロディ）+ ランダム生成 + 汎用肖像プール
    events-data.ts  全イベント定義（自動43+選択4。arrival=統合到着抽選、expedition/trade_offer/power_crisis/stockpile_crisis=選択、期間修正系はmutate+addModifier）
src/ui/       React UI
  screens/    TitleScreen / PlayScreen
  components/ UnitCard, DecisionBoard（procureトグル）, StatusWall, CompactStatus, GameMenu, *Overlay 群 等
  hooks/      usePlayback（ビート再生）, useTokenDnD（Pointer Events DnD）
  art/        PixelArt + manifest（アートスロット。PNGを自動検出）
  assets/     PNG アセット（{kind}/{id}.png に置くと自動反映）
  store.ts    zod スキーマ + セーブ整合性 / StoreProvider / settings
tests/        Vitest（gameコア単体・決定性・simulation・UIスモーク・playback）
```

### 中核の契約

- `step(state, action)` は**純粋**。同じ seed + 同じ行動 = 同じ結果（決定性テストで保証）。
- 1日の解決は `commitDay` → 任務効果 → 清算（procure・食料/士気減衰・探索帰還解決含む）→ イベント → 日付進行。
- **選択イベント**: 発火すると `phase:'choice'` で一時停止し、`resolveChoice` アクションで再開する。探索は `perUnit` で動的選択肢を生成。
- 再生（`usePlayback`）は**ビート単位**: `flow`（定常増減=レポートへ流す）/ `event` / `arrival`（後者は**スポットライト=ポーズ+カード+続行**）。探索帰還は `event:expedition_return` + `unit:{id}` で arrival 扱い。

## 主要な設計決定（要約）

- **作業員→ユニークユニット**: 名 + 二つ名 + 4適性(労/技/医/魅) + 特性 + フレーバー。配置は「誰をどの任務へ」の一段。
- **ユニークパロディ**: 30体を実在人物（故人）15 + アニメ/ゲームキャラ15のパロディに。名前は1文字ズラし、フレーバーは元ネタ参照。初期4体はオリジナルのまま。
- **統合到着抽選**: 30体ユニークプール + ランダム生成の混合抽選。重みは日数逓増、確定受け入れ、上限10、ユニークは重複なし（`flags.joinedUniques`）。
- **汎用肖像**: ランダム加入者に8枚プールから決定的割り当て（seed+unitId ハッシュ、RNG非消費）。
- **選択型イベント**: `EventDef.kind:'choice'` + `choices`/`perUnit`。発火で `phase:'choice'` 停止、`resolveChoice` で再開。
- **探索の経済**: 備蓄前払い・数ターン不在・毎日抽選帰還・適性で大成功(4日分)/成功(1日分)。不在ユニットは清算・表示から除外。
- **期間修正（Modifier）**: ターゲット文字列×演算（mult/add/set）の汎用機構。参照側にクエリ点を追加するだけで新効果に対応。
- **v1.3 経済再編**: 予算を唯一の希少ハブに。食料は減衰で「漏れるバケツ」、備蓄は procure/buy_stockpile で「貯める対象」、士気は炊き出し予算化で「予算競争の一員」。foodPerUnit は熟練狙い撃ちのデカップリングレバー。
- **v1.4 アクティブ難易度**: アクト制=予定されたエスカレーション（境界で Modifier 冪等追加・遷移告知）、派生脅威度=余裕への緩やかな課税（threat トーンの weight スケーリングのみでベース weight は不変）。イベント総数・1日1件のペースは不変、純粋・決定的を維持。
- **バランス**: `BALANCE`（`src/game/data/balance.ts`）に一元化。実測 熟練〜53%/無作為〜0-1%（v1.4・硬派）。期間の日数・倍率は `BALANCE.modifier`、脅威度・アクトは `BALANCE.threat` / `BALANCE.acts`。

## 次のステップ（未着手・候補）

1. **ユーザー報告「変更が反映されていない」の診断 + UI ブラッシュアップ（次回最優先・ユーザー依頼）**:
   - 経緯: 人員表示の収まり修正（コミット `7db8d5c`）後、ユーザー側で旧表示の疑い。確認ずみの事実: dev 両ポート（5173/5199）とも修正後モジュールを配信（curl で `pitchFor`/`fitSize` の存在確認）+ 10人セーブのヘッドレス描画で修正を目視確認。**Phaser は HMR で再初期化されないため開きっぱなしのタブは硬再読込が必要** / 公開 GitHub Pages は push 禁止のため移行前 React 版のまま
   - 次回手順: ①ユーザーが参照していた URL/ビルドを特定 ②dev なら硬再読込またはサーバ再起動で再確認 ③それでも駄目ならユーザー視点でスクショを撮り、ヘッドレス再現（10人セーブ: 初期4+汎用6 を `SAVE_VERSION=5` で serialize し localStorage へ注入）と比較 ④原因にかかわらず下記ブラッシュアップを実施
   - **UI ブラッシュアップ候補**: 配置時の群れの可読性（グリフ角箱の重なり。T2 トークンスプライト到着時の見栄え含む）/ wide トレイ48px行の見た目の収まり（バッジのはみ出し・縦中央）/ narrow の詳細シートとトレイの重なり具合 / プレースホルダ全体の見た目の底上げ（施設グリフ・地面プレートの仮描画）
2. **Issue #1 P6 の完走（人間の判断待ち）**: a11y（キーボードフル操作・aria-live 告知ブリッジ）/ エンディング演出の新方針化 / WB-10 イベント画像優先リスト / `package.json` の version と docs の v2.0 記録 / Issue #1 クローズ。P2「要実機確認」リストの実機消化と T2 アセット調達方針・公開スケジュールの判断が前提。実行手順は docs/24 §2.6・受け入れは docs/22 §11
3. **T2 アセット制作（25点・docs/23）**: 「アセット待ち」。`src/assets/{town,facility,token}/` へ配置すればローダーが自動反映（ゲートは docs/23 §3）
4. **v1.4 D5（任意）**: 予告・連鎖の拡張（typhoon 予報 / landslide_warning→actual の応答窓 / アクト HUD ラベルの強化）。バッジ・ビートは D2 で実装済み。設計は docs/20 §4 D5
5. **v1.2 新規イベント22件のアート制作**: すべてプレースホルダ表示。WB-10 の優先リスト（上記 P6）に合流。`assets/event/{id}.png`（128×80）を配置すれば自動反映。孤立4枚（refugees 等）は流用候補
6. **エンディングの成果/成績ベース分岐**（`docs/16 §8` P3・延期済み）: 誰も死なない/◯人死亡/特定ユニーク/成績集計で分岐
7. 拡張（`docs/04`: 地区/派閥/交易/外交 等。**音響・BGM は構想あり → docs/25**。Issue #1 クローズ後が着手条件。fx対応表は `sfx` フィールド追加に対応済み）

## Issue #1 実行記録（町中心UI・Phaser 移行）

- **採用バージョン**: Phaser **3.90.0**（`^3.90.0`。npm の latest は 4.x だが docs/22 §3.2 の決定どおり 3 系に固定）
- **P1 完了**（コミット `3ff19ac` store純TS化 / `e6d4f21` Phaser骨格）:
  - `src/store/` 新設（zod スキーマ・save/load・history・settings の純粋コア。`SAVE_VERSION=5` 不変）。`src/ui/store.ts`・`src/ui/settings.ts` は委譲する薄い React バインド。`tests/store.test.ts` は `src/store/` を import
  - `src/scene/` 新設: Game 設定（Scale FIT + CENTER_BOTH・`pixelArt`/`roundPixels`・`type: AUTO`・レターボックス `#0a0e24`）/ Boot・Title・Play シーン骨格 / `import.meta.glob` アセットローダー（P3 まで `src/ui/assets` を読む）/ デバイスクラス判定（wide ≥900px / narrow <900px）と `scale.setGameSize` による設計サイズ切替（1280×720 / 480×854）+ リレイアウトフック / UIキット基礎（Panel・Button・Gauge・Text、docs/07 §2.2–2.4 トークン踏襲）/ フォントは `document.fonts.load` 完了待ち
  - **dev 入口は `phaser.html`**（dev 専用の独立ページ。本番エントリ index.html / main.tsx は P3 まで無変更）
  - **lint 強制を追加**: `src/scene/`・`src/store/` は React import 禁止、`tests/` は Phaser（と Phaser 依存シーンモジュール）の import 禁止
  - manifest は移行期 `src/scene/art/manifest.ts` が `src/ui/art/manifest.ts` を re-export し両者の一致を保証（P3 で統合）
  - 検証: 全コマンド緑 + ヘッドレスブラウザで骨格起動・wide/narrow 往復・React 版の save/再開/settings 不変を確認
- **要実機確認**: なし（P1 の範囲はヘッドレス検証で充足）
- **次の一手**: P2（Play シーンの等価構築・プレースホルダ）

### P2 完了（コミット `89698c3` / `f2996e5` / `0212ab2` / `b4289c1`）

- **コミットA**: 町領域（`town/layout.ts` のアイソメプロット=画像トラックと共有する正・`deriveFacilityView`）/ 人物トレイ（待機+探索中）/ UnitToken / 配置モデル（DnD+タップ二段階・preview・`taskCost` 系判定を再利用）/ `SceneStore`（自動セーブ維持）
- **コミットB**: HUD（日数・資源・警報・modifier バッジ・undo・メニュー）/ 計画ストリップ（autoAssign・解除・確定・ration/procure トグル・予算不足表示）/ 詳細パネル（narrow は切替式シート、WB-9）/ ユニット詳細オーバーレイ / 折り畳みログドロワー / Escape で閉じるメニュー / 未配置確定の確認ダイアログ / `docs/22 §4.5` の2レイアウト + resize 往復 + セーフエリアプローブ
- **コミットC**: `src/scene/playback/`（`buildBeats`・`UI_TIMING`・`PlaybackController`、ビートモデル不変）+ 純TSテスト `tests/scene-playback.test.ts` にカバレッジ引継ぎ（167件緑）/ 選択・加入帰還・イベント再生・エンディングのオーバーレイ / スキップ / タイトルに `canResume` 判定・ブリーフィング（スキップ可）・演出トグルを移植
- **fix**: narrow ストリップのボタン幅溢れ修正
- **ヘッドレス検証ずみ（付録A相当）**: 4任務の配置（DnD+タップ二段階+差し替え+取消）/ autoAssign / commit（未配置ガードの確認ダイアログ含む）/ undo / skip / 選択イベント（power_crisis を実操作、残り2種は同コードパス）/ セーブ→リロード→再開（choice フェーズ途中の再開で選択オーバーレイへ復帰）/ 続きから vs 最初から / 新規開始確認 / メニュー（Escape 閉じ）/ 演出 OFF で playback 抑制が即時反映 / 警報・ログ・詳細パネル / wide↔narrow 往復
- **要実機確認（P2 出口・人間の実機確認が必要）**:
  1. 探索の送出→帰還→スポットライト（perUnit 選択肢・備蓄不足/探索中ガード含む。発火が抽選依存のためヘッドレスで未到達）
  2. 加入スポットライト（arrival ビート。同上）
  3. エンディング4種 + もう一度/タイトルへ（実プレイまたは長時間操作が必要。描画コードは選択/帰還と共通パス）
  4. 実機 SP のタッチ操作（narrow の DnD・長押し/ダブルタップ抑制・ノッチ端末のセーフエリア）
  5. 実機での画面回転・リサイズ（ヘッドレスは viewport 変更で代用ずみ）
  6. WebGL 無効環境の Canvas フォールバック（`type: AUTO`）
  7. 実機 360px 幅での本文 13px 可読性 / `prefers-reduced-motion` の現状記録（P6 完成前提）
  8. キーボードのみでの操作現状記録（P6 目標）
- **決定記録（P2）**: セーフエリア無しの環境では `§4.5` 寸法表と完全一致、inset 存在時のみ 8px ガードを適用 / narrow のストリップは短縮ラベル+幅縮小で 480 に収斂 / manifest は P3 まで `src/scene/art/manifest.ts` 経由の re-export で一致保証
- **次の一手**: **P3 は公開版切替を伴うため人間の GO が必要**。上記要実機確認の消化と GO 待ち

### P3 完了・React 完全撤去（コミット `58ce6ba` `5227fdf` `3d21f4d` `892d4ea` `f0f20f1`）

- 純TSテスト追加（town 導出・layout・regions・tokens・plan）→ アセット移管（`src/assets/`・manifest 所有権は `src/scene/art/`）→ エントリ切替（index.html→`src/scene/main.ts`・`viewport-fit=cover`）→ `src/ui/` と React 依存の完全削除
- **検証**: 全コマンド緑（テスト148件）/ セーブラウンドトリップ（v5 セーブが Phaser 版で復帰・choice 途中含む）/ **WebGL 無効時の Canvas フォールバック起動**（`type: AUTO`）/ 生産エントリで計画〜確定〜再生の往復
- **記録**: 本番バンドル約1MB（Phaser。docs/22 §3.3 のとおり許容・chunk 警告は想定内）/ `src/game/`・`SAVE_VERSION=5` は全工程無変更
- **次の一手**: P4（T2 不在のためグリフフォールバックで継続。画像接続は「アセット待ち」記録）

### P4 完了（グリフフォールバック。T2 は「アセット待ち」）

- **入口ゲート記録**: T2 成果物は未納品（`src/assets/facility/`・`token/`・`town/base` 不在）。docs/24 §0 のとおりグリフフォールバックで継続し、画像接続部分は実装済み・アセットを置けば自動反映
- **トークン解決**（`src/scene/town/token-resolve.ts`、docs/23 §1.4 の順）: `token/{portrait-id}` → 汎用プール（`selectRandomPortrait` 流儀の決定的割り当て、seed=0）→ グリフ。テストで決定性を保証
- **施設スプライト差替**: `deriveFacilityView` の状態ID → `facility/{id}-{state}` をローダー経由で解決。未着時はグリフプレースホルダを維持（P2 と同表示）
- **検証**: 153件緑 + ヘッドレスでグリフ表示を確認 / 並べ置きゲート（docs/23 §3.2）はベース画像不在のため「アセット待ち」
- **次の一手**: P5（fx対応表・網羅性テスト・プレイバックの町演出・環境表現）

### P5 完了（コミット `5bf6fee`）

- **fx対応表** `src/scene/town/fx-map.ts`: (source, target) → {施設, 演出種} の写像テーブル。将来の `sfx` フィールド追加に対応した形（docs/25 先行準備）
- **網羅性テスト**: 既知 source（task 6・settlement・アクト2・全イベント・expedition_return）を列挙して対応表と突合（157件緑）
- **プレイバック接続**: flow ビート→施設への浮遊表示+パルス、arrival ビート→町の入口（road）の出現演出。`reducedMotion` で抑制
- **環境表現**: 電力水準→町の灯り（明度オーバーレイ）/ 危険→赤ティントパルス / 台風→雨レイヤー・寒波→シアンティント / アクト連動の空（WB-6/WB-8 の限定範囲）
- **次の一手**: **P6 入口で停止**。T2 アセット不在のため画像接続は「アセット待ち」。P6（a11y・周辺画面・受け入れ確認・v2.0 記録）は人間の判断（アセット調達方針・公開スケジュール）を要するため報告して停止

### P5 後のfixと未解決レポート（コミット `7db8d5c`・セッション終了時）

- **fix 内容**: 人員表示の収まり — 肖像PNG（48×48正方）を3:4枠へ強制して縦伸びしていたのを `fitSize` でアスペクト保持・足元揃えに修正（町トークン・トレイ・詳細・スポットライト共通）/ 待機トレイは人数に応じてピッチ自适应（タッチ下限44）+ wide トレイ 440→480・ストリップ 840→800（10人収容を確認）/ 配置時の群れは浅い2–3列の列配置へ
- **検証**: 10人セーブ（初期4+汎用6、day3）を localStorage 注入して wide/narrow をヘッドレス確認。157件緑
- **未解決**: fix 後もユーザーから「反映されていない」の報告。dev 両ポートの配信とヘッドレス描画は修正済みを確認ずみのため、開きっぱなしタブ（HMR 非対応）または公開 Pages（旧 React 版）の閲覧が有力。**次回セッションの最初に「次のステップ」1 の診断を行うこと**
- **状態**: ここでセッション終了。作業ツリーはクリーン（全コミット済み・**未 push**）、dev サーバは終了時に停止ずみ

### 継続セッション（ブラッシュアップ + ユニットグラフィック採用）

- **UI ブラッシュアップ**（コミット `f0c13cd`・157件緑）: 地面プレートの描き込み（山並み2層・等高線・プロット菱形塗り・施設間の小道・右端出口への道）/ 町トークンは軽量グリフ化（後列が透けて群れの可読性向上・負傷は赤枠）/ トレイはデバイスクラス対応（wide=1行中央・0.8倍・バッジはトークン内側・探索は「探」バッジ減光で1行に混載 / narrow=従来のヘッダー2段）/ 状態遷移時のバッジ・インタラクションを整理（探索⇔待機の切替で復帰）
- **ユニットグラフィック採用**（コミット `075f4db`・158件緑・**ユーザー要望**）: トークン解決順を **`token/{id}` → `portrait/{id}`（既存ユニットグラフィック）→ 汎用プール → グリフ** に変更（docs/23 §1.4 の順序をユーザー判断で中間挿入。T2 トークン未着でも全ユニットの肖像が表示される）。T2 到着時は `token/` が優先され自動で切替わる。テスト追加
- **状態**: dev サーバ `http://localhost:5199/` 起動中（タブは硬再読込が必要）。**未 push**

### UI ブラッシュアップ（ユーザー報告3件・2026-08）

- **左上の謎のボタン**（コミット `671a3ef`・179件緑）: `UnitDetailsOverlay` の「閉じる」ボタンが `show()` まで `content` に追加されず、PixelButton 生成時のシーン直下所属のまま (0,0) に浮遊・常時描画（初回ユニット詳細表示まで画面左上に半欠けのボタン。ヘッドレスでラベル位置 (-23.5,-7) を実測）。生成直後に `this.add()` でカードコンテナへ所属させ、show() 内の再親化は従来どおり（begin() の removeAll で破棄されるため content 直接追加は不可）。併せて ModalCard の暗幕（dim）を生成時に非表示へ
- **イベント後のスキップ混入**（コミット `5146fcf`）: skipButton が flow ビート中に町領域の**下端中央**（イベントカードの続けるボタンの直近）へ出現していた / 町領域の**右下コーナー**（wide 1180,484 / narrow 380,400）へ移動 + 最終ビートでは非表示（表示条件 `busy && !waiting` は維持）
- **文字の読みにくさ**（コミット `57d6c62`）: ① Phaser 3.90 は `resolution` オプション**非対応**（型定義・ScaleManager ソースで確認）のため、**ゲーム内部サイズを devicePixelRatio（上限2）倍**にして HiDPI で CSS 縮小表示（整数比のため再サンプリングなしで鮮明化。1× 環境は無変化。gameSize が dpr 倍になるが全レイアウトは gameSize 相対で一貫）② TEXT_SIZE +1px（本文 16/14・ラベル 13/12。narrow 配給ON「配給:節約」62px vs 64px ボタンで収まり確認）③ inkDim #9aa3cc→#a8b1d9 ④ ハードコードの施設ラベル 11→13・fx 浮遊テキスト 14→16・トレイヘッダ 12→13・「探」バッジ 11→12 を TEXT_SIZE 参照へ統一
- **検証**: ヘッドレスでシーン検査（浮遊ボタン消滅 / skip 位置・表示条件 / canvas バッファ 2× / 文字サイズ分布 / narrow ラベル溢れなし）。**視覚の最終確認は実機が必要**（硬再読込 or サーバ再起動。dev は現在 `http://localhost:5173/` で起動中）

## 規律・注意点

- **フェーズごとにコミット**（`docs/11` 参照。1フェーズ=1コミット、CI緑を条件に）
- **数値は `balance.ts` に集約**。ロジックにマジックナンバーを書かない
- **コードにコメントは原則追加しない**
- **韓国語混入に注意**（過去に `소식`/`균형`/`바로` が混入した）。懸念があれば `[가-힣]` でgrep。中国語漢字（「游戏」等）の混入も過去に起きたため、不自然な漢字に注意。grep は PNG バイナリを誤検出するので `--include='*.ts' --include='*.md'` 等でテキストに絞ること
- セーブは zod スキーマ（`SAVE_VERSION=5`）。非互換の旧セーブは破棄→新規ゲーム
- `src/game/` は DOM非依存（lintで強制）。UIからコアへの一方向依存を守る
- **バランス調整の知見（v1.3 E4）**: random と skilled は食料で強く相関し、汎用レバー（food.decay/income/士気）は両方を同方向に動かす。skilled のみを下げるには foodPerUnit（人員比例）が有効。救済イベントは食料逼迫構成では熟練も恩恵を受けデカップリング不可。詳細は docs/19
- **バランス調整の知見（v1.4 D4）**: slack 課税（イベント構成比）は autoAssign を 60% 以下に抑えるほど強くできない（threat は資源を直接奪わないため）。skilled の天井を実際に決めているのは静的な食料圧。アクト倍率の強化は熟練より FR 番人を先に壊す（autoAssign は分散で耐え、FR は絶対値が必要）。無作為の生存は配置由来の食料収入で頭打ちになり、許容レバー内では 5% に届かない（v1.3 の 3% も標本誤差、400ゲーム真値は両者 1% 前後）。**60ゲームの熟練率は ±6pt 程度の種子噪声**で、近い設定間の比較は 400ゲーム測定でないと分解能不足。詳細は docs/20 §8
