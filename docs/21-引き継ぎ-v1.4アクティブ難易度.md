# 引き継ぎ: v1.4 アクティブ難易度（docs/20）

**完了済み（2026-08-05 第2セッション）** — D1–D4 実装・確定。経緯と知見は docs/20 §8、現行バランスは docs/00 を正とする。以下は第1セッション（計画確定時まで）の記録。

## このセッションで実施したこと（コミット順）

1. `b3fc9f8` — **full_recovery 到達性の決着**（v1.3 の残課題）。狙ったプレイの証拠テスト `tests/full-recovery.test.ts` を追加（熟練simと同じ seed で **37/60** が full_recovery 到達、`>=10` を表明）。バランス値・イベントは無変更。決着記録は docs/19 §7 と docs/00
2. `d0417a1` — **v1.4 計画の確定**: `docs/20-アクティブ難易度.md`（派生脅威度 + アクト制。D1–D5 マイルストーン。未着手）+ README 目次更新

## 完了記録（第2セッション・2026-08-05）

コミット順: `6448d4b` D1 脅威度の骨格 → `285dcc5` D2 アクト機構（FR 番人に従いアクト倍率を緩和）→ `51a0ceb` D3 sim stats 拡張（round 1: 熟練30% / 無作為0% / FR11）→ `e21f6a8` D4 確定値（熟練53% / 無作為 0/40・真値1/400 / FR19）→ docs 更新コミット。

- **確定値**: `food.decay:3` / `foodPerUnit:4.5` / `threat:{scale:0.2, cap:2.0, actBase:[0,1,2], slack:{foodDays:6, powerAt:75, medicalAt:75, moraleAt:75, stockpileAt:55}}` / `acts:{stalemate:{start:11, powerDecayMult:1.1}, final:{start:21, powerDecayMult:1.25, medicalDecayMult:1.15, incomeMult:0.9}}`
- **tone 分類**: 自動イベント43件 = threat 22 / boon 20 / arrival 対象外（§2.2 リスト外の elderly_illness・ration_protest は効果の向きから threat）
- **レッドライン**: 熟練 ≤60%（53% ✓）/ FR ≥10（19 ✓）/ slack 不変条件 ✓。**無作為 5–10% は未達**（構造的に到達不能と判定。docs/20 §8 に証拠）
- **D5 は未着手**（任意）: typhoon 予報 / landslide 応答窓 / アクト HUD ラベル強化
- **追試のコマンド**: `npm run sim -- --reporter=verbose`（threat 発火率・slack バケットをログ出力）。400ゲーム測定は `tests/simulation.test.ts` の `GAMES` を一時的に変更

## 保全すべきベースライン（D3/D4 の sim 比較基準）

| 項目 | 値 |
| --- | --- |
| 熟練（autoAssign+低適性探索、60ゲーム） | 生存 **28/60（47%）** { full_recovery:0, managed_sacrifice:18, self_governance:10, collapse:32 } |
| 無作為（40ゲーム） | 生存 **1/40（3%）** { managed_sacrifice:1, collapse:39 } |
| FR 狙い打ち戦略（60ゲーム、tests/full-recovery.test.ts） | **full_recovery:37** / managed:9 / self_gov:1 / collapse:13 |
| テスト数 | 138件（全緑） |
| イベント発火/modifier 統計 | `npm run sim` のログ（docs/19 冒頭にも記録） |

## 次の作業

**docs/20 §4 の D1 から開始**。実装アンカー:

- **D1 脅威度の骨格**: `EventDef` に `tone?: 'threat' | 'boon'`（types.ts:138 の型のみ。GameState 無影響）/ 自動イベント42件の分類（docs/20 §2.2 のリスト）/ `BALANCE.threat` 新設 / `src/game/threat.ts` 新設（`threatLevel` / `actOf`、DOM非依存）/ `determineDayEvents`（events.ts:32-39）の自動イベント weight にスケーリング適用
- **D2 アクト機構**: `BALANCE.acts` / `finalizeDay`（engine.ts:63）で現在日のアクト Modifier を**冪等に追加**（`addModifier` の id 重複排除を利用。境界イベント方式ではなく毎日の存在保証方式）/ 新規追加時のみ遷移 effect
- **D3/D4 sim 確定**: `tests/simulation.test.ts` の stats に threat トーン発火率（アクト別）・slack 到達日数を追加。round 1（A+B そのまま）→ round 2（静的食料圧の緩和 × slack 課税の反復）。**前後比較を必ず docs/00 に記録**（規律）
- **DoD / レッドライン**: docs/20 §7。熟練 ≤60%（お任せクリアの定常化を許さない）/ 無作為 ≥5%（目標 5–10%、結果を見て判断）/ 不変条件「slack ランの生存率 ≥ 非 slack ラン」/ FR 番人 ≥10

## ユーザー確認済みの決定（詳細は docs/20 §0）

- バランス数値は結果を見て判断。ただし**お任せ配置でクリアが定常化するのは不可**（レッドライン 60%）
- **不快にしないこと**を最優先（予告・cap ×2.0・バッファ直接没収の禁止）
- FR 番人の扱いは一任（現行 `>=10` を維持する方針で採用済み）
- 破壊的変更は許容（旧セーブは破棄→新規ゲーム）。ただし本計画は **SAVE_VERSION=5 据え置き**で実現できる見込み
- act3 の収入減（`income:budget ×0.9`）は採用

## このセッションで得たノウハウ・注意点

- sim の `console.log` は `npx vitest run --testNamePattern simulation --reporter=verbose` でないと表示されない
- `tickModifiers`（modifiers.ts:29）は `day == startDay` の日は decrement しない。アクト境界の実効日は必ずテストで確認する
- `addModifier`（modifiers.ts:35）は id で重複排除 → D2 の冪等保証の要。旧セーブの途中再開（例: day 15）からも初回 finalize で補完される設計
- 選択イベントの optionId はイベントをまたいで一意（buy_food / divert_medical / distribute / send_* 等）。id の集合でイベントを判定できる（`tests/full-recovery.test.ts` に実例）
- CI は `format:check`（prettier）も走る。コミット前に `npm run format:check`
- **中国語・韓国語混入**: このセッションで docs/20 に簡体字「设计」が混入し検出・修正した。ドキュメント作成後は `grep -P "[가-힣]"` と不自然な簡体字の grep を習慣にする
- `tests/full-recovery.test.ts` は番人: アクト圧・脅威スケーリングが FR 率を下げると assertion が検出する（docs/20 §2.4 の設計意図と照らして判断）

## 触らないもの（v1.3 からの継続制約 + docs/20 非目標）

- 道路復旧の coef（0.1 で生存率25点揺れるナイフエッジ）
- 探索の経済（cost・帰還率・報酬）
- 既存イベントの**ベース weight**（docs/20 は threat トーンへの乗算スケーリングのみで、weight 関数自体は触らない）
- 選択イベント4件の weight（v1 のスコープ外）

---

## 次の作業者に渡す依頼プロンプト（コピペ用）

**使用済み（2026-08-05 完了）** — 記録として残す。以下をそのまま次のセッションの冒頭に貼る。

```
孤立した町の30日間 プロジェクトの作業依頼（v1.4 アクティブ難易度の実装）

まず AGENTS.md の誘導に従い docs/00-現状と引き継ぎ.md と docs/README.md を読んで現状を把握してください。
その上で、今回の作業の特化引き継ぎ docs/21-引き継ぎ-v1.4アクティブ難易度.md と、設計計画 docs/20-アクティブ難易度.md を必ず読んでから着手すること。docs/21 にベースライン数値・実装アンカー（file:line）・ノウハウ・触らないものがまとまっています。

背景
- v1.3 経済再編（docs/19 / E1–E4）は完了・コミット済み。バランスは 熟練 47% / 無作為 3%
- full_recovery 到達性は決着済み（docs/19 §7）。tests/full-recovery.test.ts は「FR 到達性」の番人テスト（現行 37/60、>=10 を表明）
- v1.4 アクティブ難易度の計画は docs/20 として確定済み（ユーザー確認済み）。まだ実装は未着手

今回の依頼: docs/20 の D1–D5 を実装し、sim で数値を確定する

進め方（docs/20 §4 のマイルストーンに従う。1マイルストーン = 1コミット、CI緑条件）
1. D1 脅威度の骨格: EventDef に tone 追加（types.ts:138 の型のみ）→ 自動イベント42件を docs/20 §2.2 のリストで分類 → BALANCE.threat 追加 → src/game/threat.ts 新設（threatLevel/actOf、DOM非依存）→ events.ts:32-39 の determineDayEvents で threat トーンの weight をスケーリング。tone 分類の網羅テスト・threatLevel の分岐テスト・決定性テストを追加
2. D2 アクト機構: BALANCE.acts 追加 → engine.ts:63 の finalizeDay で現在日のアクト Modifier を冪等に追加（addModifier の id 重複排除を利用。modifiers.ts:29 の tickModifiers は day == startDay で decrement しない点に注意し、実効日をテストで確認）→ 新規追加時のみ遷移 effect。旧セーブ想定の途中再開（例: day 15）の補完テストも追加
3. D3 sim 確認 round 1（A+B そのまま）: tests/simulation.test.ts の stats に threat トーン発火率（アクト別）・slack 到達日数を追加。熟練・無作為・ending分布・FR番人を測定し、docs/21 のベースライン表と前後比較。不変条件「slack ランの生存率 ≥ 非 slack ラン」を確認
4. D4 sim 確認 round 2（デカップリング反復）: 静的食料圧（foodPerUnit / food.decay）の緩和と slack 課税の再調整を反復し、レッドライン内への着地を狙う
5. D5（任意・余裕があれば）: 予告・連鎖の拡張（typhoon 予報 / landslide 応答窓 / アクト HUD ラベル）

レッドライン（超えたら数値を戻して再調整。ユーザー確認済みの方針）
- 熟練生存率 ≤60%（お任せ配置 autoAssign でクリアが定常化するのは不可）
- 無作為生存率 ≥5%（目標 5–10%。最終値は結果を見て判断）
- 不変条件: slack ランの生存率 ≥ 非 slack ラン（逆転したら課税が強すぎて設計失敗）
- FR 番人: tests/full-recovery.test.ts が緑（>=10）。割れたらアクト倍率が行き過ぎのシグナルとして緩める

やってはいけないこと（docs/21「触らないもの」と同じ）
- 道路復旧の coef 変更 / 探索の経済（cost・帰還率・報酬）の変更
- 既存イベントのベース weight 関数の変更（threat トーンへの乗算スケーリングのみ）
- 選択イベント4件の weight 変更
- SAVE_VERSION の引き上げ（docs/20 は SAVE_VERSION=5 据え置きで実現する設計。型変更が必要な場合は立ち止まって報告）

規律（docs/00 より）
- フェーズごとにコミット（npm test && npm run typecheck && npm run lint && npm run format:check が緑であること）
- 数値は src/game/data/balance.ts に集約（BALANCE.threat / BALANCE.acts。ロジックにマジックナンバー禁止）
- コードにコメントは原則追加しない
- src/game/ は DOM非依存（lintで強制）
- 韓国語・中国語の混入に注意（[가-힣] で grep。docs/20 作成時に簡体字「设计」が混入して検出・修正した実例あり）

完了の定義（docs/20 §7 DoD）
- D1–D2 実装、全テスト緑 / typecheck / lint / format / build 緑
- sim 前後比較（熟練・無作為・ending分布・threat 発火率・FR番人）を docs/00 に記録
- レッドライン内で着地し、確定値を balance.ts に反映
- docs/00 の「次のステップ」と docs/README の実装状況を更新。docs/20 の状態を「計画中」→「実装済み」へ更新し、必要なら docs/21 に完了記録を追記（または新規引き継ぎへ交接）
```
